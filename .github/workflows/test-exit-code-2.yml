# Test: generic_failure (exit code 2)
name: "Test - Exit Code 2"

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/test-exit-code-2.yml'

jobs:
  exit-code-2:
    runs-on: ubuntu-latest
    steps:
      - name: Exit with code 2 (misuse of shell command)
        run: |
          echo "Simulating command misuse..."
          exit 2
